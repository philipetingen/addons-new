<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Purchase Order Form: Show DM fields -->
    <record id="view_purchase_order_form_dm_fields" model="ir.ui.view">
        <field name="name">purchase.order.form.dm.fields</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <!-- Hidden field for visibility condition -->
            <xpath expr="//field[@name='partner_id']" position="before">
                <field name="is_dm_order" invisible="1"/>
            </xpath>
            
            <!-- Add DM Deal reference in header -->
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="dm_deal_id" readonly="1" 
                       invisible="not dm_deal_id"
                       options="{'no_create': True, 'no_open': True}"/>
            </xpath>
            
            <!-- Add package-native columns to inline order line tree -->
            <xpath expr="//field[@name='order_line']/tree//field[@name='qty_received']" position="after">
                <field name="qty_received_packages" string="Received (Pkg)" 
                       readonly="1"
                       optional="show"/>
                <field name="packaging_qty_dm" string="Ordered (Pkg)" 
                       readonly="1"
                       optional="show"/>
                <field name="packaging_price_unit" string="Pkg Price" 
                       readonly="1"
                       optional="hide"/>
                <field name="amount_package_native" string="Pkg Amount" 
                       readonly="1"
                       optional="hide"/>
                <field name="is_dm_line" column_invisible="1"/>
            </xpath>
        </field>
    </record>

</odoo>